# Origin

This folder is a **snapshot** of Query Hub from the PAR EDT Hub monorepo:

`edt-hub/server/src/tools/query-hub/`

Upstream changes are not auto-synced. Merge fixes manually or re-copy when needed.
