# Query Hub — local memory

**Purpose:** Handover and project-specific context for **Query Hub only**. The repo root `.memory/` holds **EDT-wide** workflow, preferences, and conventions.

## Files

| File | Use |
|------|-----|
| **`HANDOVER.md`** | Rolling “where we left off” for Query Hub. Update after each task that touches this tool. |

## When to update

At **end of each prompt/task** that changes Query Hub (code, API, client, or this tool’s README), refresh **`HANDOVER.md`** so another session or tool can resume without replaying the chat.

Root **`.memory/HANDOVER.md`** is only for **repo-wide** focus (multiple tools, shell, shared infra).
